server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/transactiondb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
